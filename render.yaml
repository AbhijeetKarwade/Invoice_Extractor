services:
  - type: web
    name: invoice-extractor
    env: python
    region: oregon
    plan: free
    branch: main
    buildCommand: bash render-build.sh
    startCommand: gunicorn app:app
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.8